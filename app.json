{
  "name": "Star Jaat Telegram Bot",
  "description": "A Pyrogram + Telethon based Telegram Bot for downloading files from channels/groups and re-uploading with Star Jaat branding.",
  "repository": "https://github.com/starjaat/telegram-bot",
  "logo": "https://telegra.ph/file/d44f024a08ded19452152.jpg",
  "keywords": [
    "telegram",
    "pyrogram",
    "telethon",
    "starjaat",
    "bot"
  ],
  "env": {
    "API_ID": {
      "description": "Get this value from https://my.telegram.org",
      "required": true
    },
    "API_HASH": {
      "description": "Get this value from https://my.telegram.org",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Get from @BotFather",
      "required": true
    },
    "MONGO_DB": {
      "description": "Your MongoDB connection string (Mongo Atlas recommended).",
      "required": true
    },
    "STRING": {
      "description": "Pyrogram string session for premium userbot (optional).",
      "required": false
    },
    "DEFAULT_SESSION": {
      "description": "Fallback userbot session string (optional).",
      "required": false
    },
    "OWNER_ID": {
      "description": "Your Telegram user ID (use @MissRose_bot → /id to find).",
      "required": true
    },
    "CHANNEL_ID": {
      "description": "Channel ID for force-subscribe (e.g., -100xxxxxxxxxx).",
      "required": false
    },
    "LOG_GROUP": {
      "description": "Group/channel ID where bot will log all uploads.",
      "required": true
    },
    "FREEMIUM_LIMIT": {
      "description": "Max batch size for free users (default: 5).",
      "value": "5"
    },
    "PREMIUM_LIMIT": {
      "description": "Max batch size for premium users (default: 50).",
      "value": "50"
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
